using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{
    //来る頻度の最大値
    float MAX_SPAN = 25;

    //ドアまでの距離
    float distanece;

    //足音が聞こえ始める距離
    float audioActiveDistance;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
